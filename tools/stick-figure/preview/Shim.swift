import SwiftUI

// Standalone previews use the app renderer without compiling the app's theme.
extension Color {
    static let ptAccent = Color(red: 40 / 255, green: 112 / 255, blue: 178 / 255)
    static let ptBrandSky = Color(red: 117 / 255, green: 183 / 255, blue: 239 / 255)
    static let ptBrandMint = Color(red: 140 / 255, green: 186 / 255, blue: 156 / 255)
}
